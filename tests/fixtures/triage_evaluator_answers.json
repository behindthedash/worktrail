{
 "_meta": {
  "description": "One real evaluator run over the triage_smoke fixture brief, recorded by `python3 -m worktrail.workqueue.triage_smoke`. Replayed offline by tests/workqueue/test_queue_triage_live_replay.py. Recorded data -- re-record by re-running the harness; hand-editing `raw_text` would make the replay assert behavior no model produced.",
  "agent": "claude",
  "captured": "2026-09-20"
 },
 "brief_id": "20260920-000000-triage-smoke-fixture",
 "focus": "`worktrail-triage-smoke` is already registered as a console script in `pyproject.toml`, so the smoke harness runs without `python3 -m`. Separately, expand `src/worktrail/workqueue/slug.py`'s module docstring to state the two truncation limits `fallback_slugify()` applies: at most 5 words and at most 60 characters.",
 "raw_text": "```json\n{\"brief_id\": \"20260920-000000-triage-smoke-fixture\", \"verdict\": \"work-directly\", \"duplicate_of\": null, \"target_change\": null, \"target_repo\": null, \"proposed_change_name\": null, \"question\": null, \"refuted_span\": \"`worktrail-triage-smoke` is already registered as a console script in `pyproject.toml`, so the smoke harness runs without `python3 -m`.\", \"corrected_span\": \"`worktrail-triage-smoke` is not registered in `pyproject.toml`'s `[project.scripts]`, so the smoke harness must be invoked via `python3 -m`.\", \"judgment_reason\": null, \"evidence\": \"Repo live, not archived (confirmed via `gh repo view --json isArchived,name -- behindthedash/worktrail` -> {\\\"isArchived\\\":false,\\\"name\\\":\\\"worktrail\\\"}). First claim refuted: `grep -n \\\"triage-smoke\\\" pyproject.toml` returns no matches, so the console script is not registered. Second claim is directly actionable and confirmed via `sed -n 1,40p src/worktrail/workqueue/slug.py`: module docstring is the single line \\\"Shared slug normalizer for work queue records.\\\" while `_SLUG_MAX_CHARS = 60` and `_SLUG_MAX_WORDS = 5` are applied by `fallback_slugify()` (both limits match the brief). Stale npm root src/worktrail/.fixtures/sample-spec (typescript 5.9.3/vitest 5.0.0 missing) is unrelated - no JS test output was used here.\", \"confidence\": \"high\"}\n```"
}
