## 1. Dashboard: expose stale task ids structurally

- [ ] 1.1 In `src/worktrail/router/dashboard.py`, add `stale_task_ids=stale_ids` to the `info.update(...)` call in `detect_stage()`'s `elif stale_ids:` branch (the impl-tasks stale-bookkeeping arm) and to the equivalent tail-tasks stale-bookkeeping arm, so both stale-bookkeeping outcomes carry the task id list as a structured field alongside the existing `next_action` string. Do not change `next_action`'s formatted text or any other existing field.
- [ ] 1.2 Add or extend a test in `tests/router/test_dashboard.py` asserting `detect_stage()` returns `stale_task_ids` (a non-empty list) when the impl-tasks stale-bookkeeping path is hit, and the tail-tasks equivalent for the tail-stale path, with no other field's value changed versus the pre-change assertions for those cases.

## 2. Drain: generic remediation-table engine

- [ ] 2.1 In `src/worktrail/drain/drain.py`, add a `StageRemediation` frozen dataclass (`key`, `label`, `finder`, `action`) per design.md D1.
- [ ] 2.2 Factor the shared body of `resume_quarantined_budget_exhausted` and `resume_verify_pending` (find → build full-real resume command → spawn → log → collect result) into a single `_resume_via_full_real(finding, agent, timeout, spawner, log, *, label)` helper, preserving the exact `["worktrail-live", "full-real", ...]` command construction and log-label substrings (`resume-quarantine`, `resume-verify-pending`) the existing tests assert on.
- [ ] 2.3 Add `sweep_remediations(repos_root, go_repo, agent, timeout, spawner, log, keys=None)` per design.md D2/D3: iterates `REMEDIATION_TABLE` (or only the rows named in `keys` when given), catches and logs a per-finding exception without aborting the sweep, and returns `Dict[str, List[Dict[str, Any]]]` keyed by each row's `key`.
- [ ] 2.4 Rewrite `resume_quarantined_budget_exhausted` and `resume_verify_pending` as thin wrappers calling `sweep_remediations(..., keys=["quarantined_budget_exhausted"])` / `keys=["verify_pending"]` respectively and returning that single key's list — public signature and return shape unchanged.
- [ ] 2.5 Update `drain()`'s two sweep call sites (pre-loop, post-loop-when-iteration-ran) to call `sweep_remediations(...)` with no `keys` filter (all table rows) instead of the two separate function calls, merging the pre- and post-pass dicts key-by-key exactly like the current `+=` merge semantics.
- [ ] 2.6 Update `drain()`'s returned summary dict to set `resumed_quarantines`/`resumed_verify_pending` from the merged sweep-result dict and add a new `resumed_stale_bookkeeping` key, per design.md D6.

## 3. Drain: stale-bookkeeping finder and action

- [ ] 3.1 Add `find_stale_bookkeeping_specs(repos_root, go_repo=None)` to `drain.py` per design.md D4 — same shape and `discover_repo_names`/`dashboard.scan` pattern as `find_verify_pending_specs`, filtering on `stage == "stale-bookkeeping"` and requiring a non-empty `stale_task_ids` (depends on Task 1.1).
- [ ] 3.2 Add `close_stale_bookkeeping(finding, agent, timeout, spawner, log)` to `drain.py` per design.md D5: for each stale task id in the finding, resolve its `TASK-*.md` path and flip `status:` to `completed` via `taskformats.devkit.schema.set_status_completed`; create a short-lived branch off the target repo's base branch (mirroring the direct fix-branch worktree pattern used elsewhere for unspecced-code changes — a plain `git worktree add`/commit/push, not the spec-worktree pipeline, since no spec artifact is being authored); commit the flipped file(s); open a PR via `gh pr create` carrying the `go:risk-low` label; tear the worktree down after the PR is open. Raise on `gh pr create` failure rather than reporting a fabricated success.
- [ ] 3.3 Register `StageRemediation("stale_bookkeeping", "close-stale-bookkeeping", find_stale_bookkeeping_specs, close_stale_bookkeeping)` in `REMEDIATION_TABLE`, after the two existing entries. Add a comment above the table noting `orchestrator-stuck` (`fanout_failed`) is intentionally excluded and why (per design.md's Non-Goals).

## 4. Tests

- [ ] 4.1 Add tests for `sweep_remediations`: table-driven sweep across all three rows on a fixture repo with one finding per stage; per-finding exception isolation (one action raises, the others still run and their results are still returned); `keys` filter restricts to only the named row(s).
- [ ] 4.2 Add tests for `find_stale_bookkeeping_specs`: discovers a stale-bookkeeping spec across repos; excludes specs whose stage is not `stale-bookkeeping` or whose `stale_task_ids` is empty; respects `go_repo` filtering — mirror the existing `find_verify_pending_specs` test structure in `tests/drain/test_drain.py`.
- [ ] 4.3 Add tests for `close_stale_bookkeeping`: flips the target task file's `status:` to `completed`; issues a `gh pr create` call carrying the `go:risk-low` label; one failing `gh pr create` does not prevent a second finding's remediation from being attempted (verified via the sweep-level isolation test in 4.1, not by retrying inside the action itself).
- [ ] 4.4 [e2e] Run the full existing `tests/drain/test_drain.py` suite unmodified and confirm every pre-existing test (in particular the `resume_quarantined_budget_exhausted`/`resume_verify_pending` direct-call tests and the `drain()`-level `resumed_quarantines`/`resumed_verify_pending` summary-dict tests) still passes without modification, per proposal.md's backward-compatibility requirement.
- [ ] 4.5 [e2e] Run `PYTHONPATH=src pytest -q && PYTHONPATH=src python3 -m worktrail.orchestrator.orchestrate check` from the repo root and confirm both pass before opening the implementation PR.
