## 1. Schema and loader (`router/policy.py`)

- [ ] 1.1 In `src/worktrail/router/policy.py`, add `_validate_routing_targets()` parsing the ordered `targets` mapping into `{name: {harness, pool, api_opt_in, auth}}`: harness must be in `SUPPORTED_AGENTS`, pool in `{subscription, free, api}`, `api` without `api_opt_in: true` appends a warning and marks the target ineligible; preserve file order. Tests in `tests/router/test_policy.py`. (Requirement: Routing declares ordered execution targets that separate harness from pool)
- [ ] 1.2 In `src/worktrail/router/policy.py`, rewrite `_validate_routing_tiers()` so cells are keyed by declared target name (`tiers.<row>.<target> = {model, effort?}`), reject cells naming an undeclared target with a warning, and parse the top-level `default_tier` string (must name a declared row). Replace `resolve_tier_map()`'s composite `(f"{tier}-{agent}", domain)` keys with `{row: {target: cell}}`. Tests in `tests/router/test_policy.py`. (Requirement: Tier rows are keyed by target and a missing cell means the target cannot serve that tier); depends on 1.1
- [ ] 1.3 In `src/worktrail/router/policy.py`, rewrite `_validate_routing_roles()` to accept only `{tier, prefer?, independent?}` (tier must name a declared row, prefer a declared target) and rename `purpose_tiers` to `purposes` in `_validate_routing_purpose_tiers()`/`resolve_routing()`; `resolve_routing()` returns `{targets, tiers, roles, purposes, default_tier, drain}` and drops `agents`/`fallback`. Tests in `tests/router/test_policy.py`. (Requirement: Roles resolve to a tier, with optional preference and independence); depends on 1.2
- [ ] 1.4 In `src/worktrail/router/policy.py`, add `_reject_legacy_routing_keys()` raising `OperatorConfigError` for `agents`, `fallback`, `drain.agent`, `drain.fallback_agents`, `purpose_tiers`, harness-keyed tier cells, and role entries with `agent_cli`/`agent_model`, each message naming the key and `worktrail-routing --migrate`; call it first in `_validate_routing()`. Tests in `tests/router/test_policy.py`. (Requirement: Legacy routing keys fail loud and migrate deterministically); depends on 1.3
- [ ] 1.5 In `src/worktrail/router/policy.py`, add `EFFORT_VOCABULARY = {claude: ..., codex: ..., opencode: None}` and a loader warning for a cell whose effort is outside its harness's vocabulary (opencode: "ignored by harness"). Tests in `tests/router/test_policy.py`. (Requirement: Effort values are validated per harness); depends on 1.2

## 2. Selector (`runtime/selection.py`)

- [ ] 2.1 In `src/worktrail/runtime/selection.py`, add `Cell` (target, harness, model, effort, pool, auth) and `select_cell(routing, tier, *, prefer=None, exclude_harness=None, capacity, now=None) -> Cell` implementing design D3 steps 1–5 on top of the existing `_capacity()` helper; `NoExecutionTarget` message lists every cell with its gate class and retry time. Tests in `tests/runtime/test_selection.py` covering prefer reorder, api opt-in skip, missing cell, soft harness exclusion, and exhaustion. (Requirement: A single selector walks a tier row across targets in preference order); depends on 1.3
- [ ] 2.2 In `src/worktrail/runtime/routing_source.py`, rewrite `routing_candidates()` to yield `{target, harness, model, tiers, purposes}` from `targets` + `tiers` (no `agents`), keeping the pure/deterministic contract. Tests in `tests/runtime/test_routing_source.py`. (Requirement: Provider/model intent has exactly one machine-wide file); depends on 1.3

## 3. Capacity and spawn (`orchestrator/agent_capacity.py`, `orchestrator/spawnlib.py`)

- [ ] 3.1 In `src/worktrail/orchestrator/agent_capacity.py`, make `provider_key(target, model)` and `check()`/`record()` key on target names, add `model_unavailable` to `DEFAULT_COOLDOWNS` (86400 s) and to `classify_failure()`'s vocabulary, and extend `gate_snapshot()` to return the failure class per gated cell. Tests in `tests/orchestrator/test_agent_capacity.py`. (Requirement: Capacity gates key on target and model)
- [ ] 3.2 In `src/worktrail/orchestrator/spawnlib.py`, change `build_cmd()` to take a `Cell`: claude `subscription` omits `--bare`; claude `api` appends `--bare`; opencode/codex unchanged apart from the model/effort from the cell. Add `build_child_env(cell, base_env)` that removes `ANTHROPIC_API_KEY` for a claude subscription cell and copies `auth.env` for a claude api cell (raise `OperatorConfigError` if unset). Tests in `tests/orchestrator/test_spawnlib.py`. (Requirement: Harness auth follows the target's pool); depends on 2.1
- [ ] 3.3 In `src/worktrail/orchestrator/spawnlib.py`, rewrite `spawn_agent(prompt, cwd, *, tier, prefer=None, exclude_harness=None, ...)` to obtain its cell from `select_cell`, record outcomes under the cell's key, and on a session-limit notice re-select with the served cell excluded; delete `_normalize_fallback_chain()`, `default_model_for_agent()`, and the `fallback_agent` parameter of `spawn_agent`/`spawn_claude_p`. Tests in `tests/orchestrator/test_spawnlib.py`. (Requirement: Per-agent default models resolve from routing only); depends on 3.1, 3.2
- [ ] 3.4 In `src/worktrail/orchestrator/spawnlib.py`, after the primary cell exhausts `retries` on an infra failure, re-select from the same row with the failed cell excluded and continue the attempt loop in the same call; return the last raw output only when every cell has failed. Tests in `tests/orchestrator/test_spawnlib.py` with a fake `subprocess.run` failing the first cell and succeeding on the second. (Requirement: An infra failure hops within the same spawn after retries are exhausted); depends on 3.3
- [ ] 3.5 In `src/worktrail/orchestrator/spawnlib.py`, when an opencode spawn fails with a top-level `UnknownError` on every retry, call `routing_cli.list_opencode_models()` (task 6.1) once and record `model_unavailable` when the cell's id is absent, else `transport`. Tests in `tests/orchestrator/test_spawnlib.py` with the listing patched both ways. (Requirement: A retired model gates its own cell with a distinct failure class); depends on 3.4, 6.1
- [ ] 3.6 [e2e] Live-verify whether codex offers a per-spawn auth selector between ChatGPT login and a stored API key (candidate: `-c preferred_auth_method=apikey`); record the finding in `openspec/changes/routing-target-selector/design.md` D6 and, if verified, lift the loader's codex-`api` rejection in `src/worktrail/router/policy.py` with a test in `tests/router/test_policy.py`. (Requirement: Harness auth follows the target's pool); depends on 1.1

## 4. Dispatch and orchestrator (`orchestrator/dispatch.py`, `orchestrator/live.py`)

- [ ] 4.1 In `src/worktrail/orchestrator/dispatch.py`, replace `agent_for()` with `tier_for(role, task, *, roles, purposes, default_tier) -> (tier, prefer, independent)` implementing the precedence in the roles requirement (explicit task tier > role tier for judgment roles > purpose > complexity > default_tier); review defaults to `(t1-deep or default_tier, None, True)`. Tests in `tests/orchestrator/test_dispatch.py`. (Requirement: Review role defaults to claude:opus); depends on 1.3
- [ ] 4.2 In `src/worktrail/orchestrator/live.py`, make `LiveSpawn.__call__` call `tier_for()` then `spawn_agent(tier=..., prefer=..., exclude_harness=<implementer harness when independent>)`, record the served cell on the journal/run record, and delete the `judgment_pinned`/`effective_fallback` branch, `_serving_agent_guess()`, the `fallback_chain`/`fallback_agent` constructor parameters and CLI flags, and every `default_model_for_agent` call (`DEFAULT_AGENT` detection becomes a default-target lookup). Tests in `tests/orchestrator/test_live_spawn.py`. (Requirement: A single selector walks a tier row across targets in preference order); depends on 3.3, 4.1

## 5. Drain, compile, skill dispatch, dashboard

- [ ] 5.1 In `src/worktrail/drain/drain.py`, build candidates from `select_cell(default_tier)` each iteration (flags `--agent/--fallback-agent` now take target names and still win entirely), pass the selected cell's model/effort to `build_command()` for the front-door session, key `record_capacity_gate()` and `capacity_gated()` on target names, reject `drain.agent`/`drain.fallback_agents` with exit 2, and run the liveness check (task 6.2) next to `validate_agent_runtime()` before the first iteration. Tests in `tests/drain/test_drain.py`. (Requirement: Drain agent selection resolves CLI over config over built-in); depends on 2.1, 6.2
- [ ] 5.2 In `src/worktrail/conductor/compile.py`, make `_default_spawn` resolve through `select_cell(routing, roles.get("compile", {}).get("tier") or default_tier)` and keep the `--agent/--model/--fallback-chain` flags of change `model-tier-routing-compile-default-spawn-policy-routing` as an explicit-cell override (flag values name a target and model). Tests in `tests/conductor/test_compile.py`. (Requirement: A single selector walks a tier row across targets in preference order); depends on 2.1
- [x] 5.3 In `src/worktrail/router/skill_dispatch.py`, resolve the dispatch cell via `select_cell(roles.get("front-door", {}).get("tier") or default_tier)` before `build_command()`, pass its model, and record skipped cells on the run record. Tests in `tests/router/test_skill_dispatch.py`. (Requirement: A single selector walks a tier row across targets in preference order); depends on 2.1
- [ ] 5.4 In `src/worktrail/router/dashboard.py`, derive the capacity line from `routing_candidates()` (task 2.2) and `gate_snapshot()` so it names each gated cell as `<target>:<model> (<class>, retry <time>)`. Tests in `tests/router/test_dashboard.py`. (Requirement: A retired model gates its own cell with a distinct failure class); depends on 2.2, 3.1

## 6. `worktrail-routing` CLI (`router/routing_cli.py`)

- [ ] 6.1 In `src/worktrail/router/routing_cli.py`, add `list_opencode_models(runner=subprocess.run) -> set[str]` wrapping `opencode models` (empty set + warning on failure). Tests in `tests/router/test_routing_cli.py` with a fake runner. (Requirement: A retired model gates its own cell with a distinct failure class)
- [ ] 6.2 In `src/worktrail/router/routing_cli.py`, add `--check`: for every cell, gate absent opencode ids with `model_unavailable` via `agent_capacity.record()`, warn on `free`-pool opencode ids lacking a `-free`/`:free` suffix, warn on effort outside the harness vocabulary (task 1.5), print a per-cell table, exit non-zero on any gate. Tests in `tests/router/test_routing_cli.py`. (Requirement: A retired model gates its own cell with a distinct failure class); depends on 6.1, 3.1, 1.5
- [ ] 6.3 In `src/worktrail/router/routing_cli.py`, add `--migrate` implementing design D9 (targets from `fallback` order, cells re-keyed, `default_tier` from the row matching `agents.*.default_model` else `t2-build`, `roles.review` → `{tier: t1-deep, prefer: <target>, independent: true}`, `drain` reduced to `max_workers`), writing `<file>.bak` first and refusing when the file already loads cleanly. Tests in `tests/router/test_routing_cli.py` migrating a fixture equal to the shipped 2026-08 file and asserting the output loads with zero warnings. (Requirement: Legacy routing keys fail loud and migrate deterministically); depends on 1.4
- [ ] 6.4 In `src/worktrail/router/routing_cli.py`, replace `STARTER_ROUTING_YAML` per design D11 (subscription targets only, all rows filled for them, commented `opencode-free` example, `default_tier`, `roles.review`, closing `--check` instruction) and have `--init` print the `--check` reminder. Tests in `tests/router/test_routing_cli.py` asserting the template loads with zero warnings and contains no uncommented `opencode/` id. (Requirement: The starter template is valid and names no opencode model); depends on 1.3
- [ ] 6.5 In `src/worktrail/onboarding/repo_init.py`, keep the `--init` starter write wired to the new template and surface `--check` in its post-write guidance. Tests in `tests/onboarding/test_repo_init.py`. (Requirement: The starter template is valid and names no opencode model); depends on 6.4

## 7. Docs

- [x] 7.1 [docs] Update `skills/worktrail-go/SKILL.md` routing section (helper output → flags mapping now `tier`/`prefer`/`independent`, no `fallback` list), `README.md` routing example, and `docs/` references to `agents`/`fallback`/`drain.agent` to the targets/rows form; add the purposes `frontend`, `backend`, `design`, `explore`, `categorize` to the worked example. (Requirement: Routing declares ordered execution targets that separate harness from pool)
- [ ] 7.2 [docs] Record in `openspec/changes/routing-target-selector/design.md` the operator follow-ups outside this repo: run `worktrail-routing --migrate` on this machine, and drop the hardcoded `--agent/--fallback-agent` from `~/bin/worktrail-drain-nightly.sh` (devops). (Requirement: Drain agent selection resolves CLI over config over built-in)

## 8. Verification

- [ ] 8.1 [e2e] `PYTHONPATH=src pytest -q` green, including the golden `PYTHONPATH=src python3 -m worktrail.orchestrator.orchestrate check` replay. (Requirement: A single selector walks a tier row across targets in preference order)
- [ ] 8.2 [e2e] On this machine: `worktrail-routing --migrate` on the live file, `worktrail-routing --check` reports the retired `opencode/x-preview-f-free` cells as `model_unavailable`, and a `worktrail-drain --dry-run` selects a `claude-sub` cell with its model on the front-door command line. (Requirement: A retired model gates its own cell with a distinct failure class)
- [ ] 8.3 [e2e] Repo-wide `rg "default_model_for_agent|fallback_agents|purpose_tiers|routing\.agents|judgment_pinned" .` returns no hits outside `openspec/changes/archive/`. (Requirement: Legacy routing keys fail loud and migrate deterministically)
