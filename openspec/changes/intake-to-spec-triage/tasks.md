## 1. Brief kind and dispatch gate

- [x] 1.1 In `src/worktrail/workqueue/work_queue.py`, add `brief_kind(frontmatter) -> "intake" | "execution"` (execution iff a non-empty `seeded-from:` value, intake otherwise, including consolidated batches carrying `## Consolidated from` or `related:`) and expose it as an additive `kind` field in `list_queue()`'s per-brief dict; cover handoff, seeded, and consolidated briefs in `tests/workqueue/test_work_queue.py`. (Requirement: Brief kind is derived from provenance)
- [x] 1.2 In `src/worktrail/router/dashboard.py`, add an `intake-untriaged` skip in `auto_pick_brief()` for every brief whose `kind` is `intake`, placed ahead of the existing gates and routed through `log_auto_pick_miss()`; cover an all-intake queue claiming nothing and a mixed queue claiming only the execution brief in `tests/router/test_dashboard.py`; depends on 1.1. (Requirement: Unattended auto-pick never claims an intake brief)
- [ ] 1.3 In `skills/worktrail-go/SKILL.md` and `src/worktrail/router/skill_dispatch.py`, route `worktrail-go <brief-id>` for an intake brief to single-brief intake triage (evaluate that brief, present the verdict, apply on confirmation via the `queue_triage` apply path) instead of claim+dispatch, leaving execution briefs on the existing claim path; cover both paths in `tests/router/test_skill_dispatch.py`; depends on 1.1 and 3.6. (Requirement: Interactive pickup of an intake brief triages it)

## 2. Candidate ranking and verdict extension

- [x] 2.1 In `src/worktrail/workqueue/queue_triage.py`, add `rank_change_candidates(brief, repo, top_k=5)` that enumerates the repo's active OpenSpec changes via `src/worktrail/router/overlap_check.py`'s `scan()` plus each change's `tasks.md` task-line tokens, scores them with the `duplicate_brief_detection` focus-overlap coefficient, and returns the top-K with id, feature summary, and open-task count — empty for `repo: null` and for a repo with no active changes; cover strong-overlap ranking, no-active-changes, and null-repo in `tests/workqueue/test_queue_triage.py`. (Requirement: Candidate targets are ranked brief-to-active-change)
- [x] 2.2 In `src/worktrail/workqueue/queue_triage.py`, extend `VALID_VERDICT_TYPES` and the verdict parser with `fold-into-change` (requires `target_change` naming a presented candidate), `propose-change` (requires `target_repo` and kebab-case `proposed_change_name`), `work-directly`, and `needs-decision` (requires `question`), downgrading any verdict with missing or invalid target fields — including a fold target outside the candidate list — to `keep` with the raw output retained as evidence; cover each well-formed verdict and each downgrade in `tests/workqueue/test_queue_triage.py`; depends on 2.1. (Requirement: Evidence-required verdict per brief)
- [x] 2.3 In `src/worktrail/workqueue/queue_triage.py`, update the evaluator prompt and per-brief context to include the ranked candidates from 2.1, the `work-directly` reproduction-evidence rule, and the `needs-decision` rule for `repo: null` briefs; assert the prompt contents in `tests/workqueue/test_queue_triage.py`; depends on 2.2. (Requirement: Candidate targets are ranked brief-to-active-change)
- [x] 2.4 In `src/worktrail/workqueue/queue_triage.py`, extend the Markdown report and JSON verdict file with counts for the four new verdict types, pull requests opened, and briefs held by the WIP cap per repo, keeping report and JSON counts identical; cover in `tests/workqueue/test_queue_triage.py`; depends on 2.2. (Requirement: Verdict file and human-readable report)

## 3. Apply actions

- [ ] 3.1 In `src/worktrail/workqueue/queue_triage.py`, implement the `fold-into-change` apply action: create a fresh worktree on a branch off the target repo's base, append a `## Folded from <brief-id>` section to the target change's `proposal.md` and a new unchecked task group to its `tasks.md`, run `openspec validate`, commit, push, open the pull request with `gh`, then close the brief with `triaged-to:` and the PR URL; any failure before the PR exists leaves the brief untouched and reports the branch name; cover success, PR-creation failure, and validation failure with a fake `gh`/git in `tests/workqueue/test_queue_triage.py`; depends on 2.2 and 3.5. (Requirement: Fold and propose are applied as a pull request, fail-closed)
- [ ] 3.2 In `src/worktrail/workqueue/queue_triage.py`, implement the `propose-change` apply action: run `openspec new change <proposed_change_name>` in the worktree from 3.1's helper, generate proposal/design/specs/tasks with the evaluator agent, validate, then reuse 3.1's branch/PR/close sequence; cover success and validation failure in `tests/workqueue/test_queue_triage.py`; depends on 3.1. (Requirement: Fold and propose are applied as a pull request, fail-closed)
- [ ] 3.3 In `src/worktrail/workqueue/queue_triage.py`, implement the `work-directly` apply action: require evidence that references a test, check, or command, stamp `seeded-from: triage:<run-date>:direct` and `recommended-route: F` on the brief in place so it stays in `queue/`, and downgrade to `keep` when the evidence lacks a reproduction reference; cover both cases in `tests/workqueue/test_queue_triage.py`; depends on 2.2. (Requirement: Work-directly converts an intake brief into an execution brief)
- [ ] 3.4 In `src/worktrail/workqueue/queue_triage.py`, implement the `needs-decision` apply action using `src/worktrail/workqueue/decisions.py`'s `pending_decision_envelope()` (subject = brief id, question from the verdict), leaving the brief queued, and skip briefs with an unresolved pending decision in later `evaluate` runs; cover filing and the skip in `tests/workqueue/test_queue_triage.py` and `tests/workqueue/test_decisions.py`; depends on 2.2. (Requirement: Needs-decision files a pending decision and keeps the brief queued)
- [ ] 3.5 In `src/worktrail/workqueue/work_queue.py`, add an optional `triaged_to` argument to `done()` that stamps `triaged-to:` in the brief frontmatter, and in `src/worktrail/workqueue/queue_triage.py` make the no-`--confirm` dry run print the planned branch, target change, and PR title for fold/propose and the planned stamp/envelope for work-directly/needs-decision without writing to the queue or any repo; cover in `tests/workqueue/test_work_queue.py` and `tests/workqueue/test_queue_triage.py`; depends on 2.2. (Requirement: Apply step never closes a brief without an approved verdict)
- [ ] 3.6 In `src/worktrail/router/policy.py`, add the `max_active_changes` key (int, default 0, non-int forced to 0 with a drift warning) and in `src/worktrail/workqueue/queue_triage.py` count the target repo's active changes at apply time and downgrade `propose-change` to `keep` plus a `## Triage <date>` note naming the cap, the count, and the top fold candidates when at or over a non-zero cap, leaving fold/work-directly/needs-decision unaffected; cover over-cap, unset cap, and fold-under-cap in `tests/router/test_policy.py` and `tests/workqueue/test_queue_triage.py`; depends on 3.2. (Requirement: Per-repo WIP cap on active changes)

## 4. Drain pre-passes

- [ ] 4.1 In `src/worktrail/drain/drain.py`, add `--intake-triage` and `--seed-backlog` flags (default off) that, before iteration 1, run the intake-triage `evaluate` + `apply --confirm` from `src/worktrail/workqueue/queue_triage.py` and the seeder from `src/worktrail/workqueue/seed_backlog.py` respectively, capturing any pre-pass failure into the summary without aborting the drain; cover both flags, a failing pre-pass, and the flags-omitted no-op in `tests/drain/test_drain.py` with stubbed triage/seeder; depends on 3.6. (Requirement: Drain pre-passes close the intake loop)
- [ ] 4.2 In `src/worktrail/drain/drain.py` and `src/worktrail/drain/summary_contract.py`, add `intake_triage` (briefs evaluated, verdicts by type, PRs opened, briefs held by cap) and `seed_backlog` (seeds captured) blocks to the drain JSON summary and the digest contract, present only when the corresponding flag is set; cover in `tests/drain/test_drain.py`; depends on 4.1. (Requirement: Drain pre-passes close the intake loop)
- [ ] 4.3 [docs] In `README.md` and `skills/worktrail-go/references/drain.md`, document the intake → change → seeded execution brief → drain loop, the two default-off drain flags, the `max_active_changes` policy key, and the `intake-untriaged` auto-pick skip reason (also add it to `skills/worktrail-go/references/auto-mode.md`); depends on 4.2.

## 5. Verification and handoffs

- [ ] 5.1 [e2e] Run `PYTHONPATH=src pytest -q`, `ruff check .`, and `ruff format --check .` and confirm all three pass; depends on 4.3.
- [ ] 5.2 [e2e] Run `worktrail-drain --intake-triage --seed-backlog --dry-run --json` against the live `$WORK_QUEUE_DIR` and confirm the `intake_triage` and `seed_backlog` summary blocks populate while `git status` in every repo under `~/projects` and the queue directory show no modifications; depends on 5.1.
- [ ] 5.3 [cleanup] Capture the operator follow-ups as `worktrail-handoff` briefs (not GitHub issues): adding `--intake-triage --seed-backlog` to the `devops` nightly drain script; choosing `max_active_changes` and `allow_seeded_implementation` for datalena; and an optional `triaged-to` dangling-PR check in stale-bookkeeping detection; depends on 5.2.
