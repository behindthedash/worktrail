## 1. Non-Goals Exclusion

- [ ] 1.1 In `src/worktrail/workqueue/queue_triage.py`, add a `_non_goal_tokens(change_dir: Path) -> set[str]` helper that reads `change_dir / "proposal.md"` and, when present, `change_dir / "design.md"`, extracts every Non-Goals / Out-of-scope section (a `## Non-Goals` heading, a `**Non-Goals:**` / `**Out of scope:**` bold-label paragraph, or a `- **Non-goals**: ...` / `- **Out of scope**: ...` bullet, each capturing text up to the next markdown heading or end of file) via a module-level regex, and tokenizes the combined matched text with the existing `_tokenize()`; missing files or no marker found yields an empty set. Cover: no marker present, missing proposal.md/design.md, a heading-form marker, a bold-label-paragraph marker, and a bullet-form `Out of scope` marker, in `tests/workqueue/test_queue_triage.py`. (Requirement: Fold candidates exclude scope-conflicting changes)
- [ ] 1.2 In `rank_change_candidates()`, after a candidate clears `_MIN_CANDIDATE_SCORE` on its existing proposal-summary/task-line overlap, additionally call `_non_goal_tokens()` on that candidate's change directory and skip the candidate (do not add it to `scored`) when its Non-Goals tokens are non-empty and `_overlap_coefficient(brief_tokens, non_goal_tokens) >= _MIN_CANDIDATE_SCORE`; a candidate with no Non-Goals tokens is unaffected. Update the function's docstring to state the exclusion. Cover: a candidate whose Non-Goals in `proposal.md` overlaps the brief's focus is excluded even though its summary/tasks score would qualify it; the same for a Non-Goals section in `design.md`; a candidate whose Non-Goals text does not overlap the brief's focus is still returned and ranked as before; a candidate with no Non-Goals section at all is unaffected (regression against the existing `TestRankChangeCandidates` cases), in `tests/workqueue/test_queue_triage.py`; depends on 1.1. (Requirement: Fold candidates exclude scope-conflicting changes)

## 2. Verification

- [ ] 2.1 [e2e] Run `PYTHONPATH=src pytest -q` and `PYTHONPATH=src python3 -m worktrail.orchestrator.orchestrate check` and confirm both repository gates pass; depends on 1.2.
