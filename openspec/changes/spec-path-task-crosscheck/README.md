# spec-path-task-crosscheck

Guard worktrail-live full-real's --spec argument against ambiguous devkit changes/ subdirectory resumption by cross-checking journal task IDs against the declared --spec path
