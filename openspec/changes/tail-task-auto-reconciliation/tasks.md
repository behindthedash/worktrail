## 1. `integrate.py`: reconciliation function

- [x] 1.1 Add `reconcile_unreconciled_tail_evidence(findings, repo, spec_id, tasks, remote, run_id, base, journal_path, pr_labels=None, route=None, gates="")` to `src/worktrail/orchestrator/integrate.py`. For each finding dict (`{"task", "worktree", "head_sha"}`), build the synthetic single-task group `{"name": f"tail-{task_id.lower()}", "tasks": [task_id], "depends_on": [], "reqs": <that task's reqs from `tasks`>}` and call `integrate_one(g, repo, spec_id, tasks, remote, run_id, base, journal_path, status, group_branch={}, quarantined={}, pr_labels=pr_labels, route=route, gates=gates)` where `status = {t["id"]: t.get("status", "done") for t in tasks}` (mirrors `finish_real`'s own `status` construction). Implements requirement: Automatic reconciliation PR for unreconciled tail evidence. Calling `integrate_one` unchanged (no new merge/conflict logic) is what implements requirement: Reconciliation reuses existing conflict and quarantine handling.
- [x] 1.2 After each `integrate_one` call, read back `journal["groups"][f"tail-{task_id.lower()}"]` (re-read the journal file rather than trusting a return value, since `integrate_one` returns `None` on both MERGED-skip and QUARANTINED) and map its `state`/`pr_url` onto that finding: `reconcile_state` in `{"opened", "already-open", "merged", "quarantined"}` (state `"OPEN"` with a freshly-created PR this call → `"opened"`; `"OPEN"` reused from a prior call → `"already-open"`; `"MERGED"` → `"merged"`; `"QUARANTINED"` → `"quarantined"`) and `reconcile_pr_url` (empty string when none). Return the enriched findings list; do not mutate the caller's input list in place — this is called with the same list `detect_unreconciled_tail_evidence` returned, and `_record_unreconciled_tail_evidence` writes whatever it's given, so returning a new list keeps the two functions decoupled and testable independently.
- [x] 1.3 Distinguishing `"opened"` from `"already-open"` needs the group's journal state *before* the `integrate_one` call in addition to after — capture `journal.get("groups", {}).get(name, {}).get("state")` prior to calling `integrate_one` for that finding, and diff against the post-call state to classify. This before/after diff plus the "already-open"/"merged" states from 1.2 are what implement requirement: Reconciliation is safe to retry across resumed runs — a repeated call reuses the existing PR or recognizes it already merged instead of re-attempting the merge.
- [x] 1.4 A single tail task producing more than one finding across repeated calls (should not happen given `detect_unreconciled_tail_evidence`'s dedup-by-construction, but guard defensively) must not raise — treat each finding independently by task id.

## 2. `live.py`: wire reconciliation into both schedulers

- [x] 2.1 In `_pipeline_scheduler` (around live.py:4122-4128), after `unreconciled_tail = integrate_module.detect_unreconciled_tail_evidence(...)`, call `unreconciled_tail = integrate_module.reconcile_unreconciled_tail_evidence(unreconciled_tail, repo, spec_id, (tail_res or {}).get("tasks", tasks), remote, run_id, base, journal_path, pr_labels=pr_labels, route=route, gates=gates)` before `_record_unreconciled_tail_evidence(journal_path, unreconciled_tail)`. Skip the call (or pass an empty list through) when `unreconciled_tail` is empty — no findings, nothing to reconcile.
- [x] 2.2 Apply the same change at the sequential path's call site in `_full_real_inner` (around live.py:4762-4768), forwarding that function's own `pr_labels`, `route`, `gates` locals.
- [x] 2.3 Verify both call sites already have `run_id`, `remote`, `base`, `journal_path`, `pr_labels`, `route`, `gates` in scope (per design.md's Context section) — no new parameter threading into `_pipeline_scheduler`/`_full_real_inner` should be needed; if any is missing, thread it from the enclosing function's own parameters rather than introducing a new default.
- [x] 2.4 Update `_format_unreconciled_tail_note` (live.py ~367-380) if its console-log wording should reflect reconciliation outcome too (optional — the journal/dashboard message is the primary surface per design.md; only change this if it can be done without duplicating the message logic that belongs in `journal_selfcheck.py`).

## 3. `journal_selfcheck.py`: reflect reconciliation outcome in the dashboard finding

- [x] 3.1 In `check_repo()` (src/worktrail/router/journal_selfcheck.py ~164-176), when building the `unreconciled-tail-evidence` finding, branch on each entry's `reconcile_state`: entries with `"opened"`/`"already-open"` produce an informational detail naming the open PR URL(s) and "awaiting merge"; entries with `"quarantined"` (or missing `reconcile_state`, for backward compatibility with journals written before this change) keep today's "needs manual/human triage" tone; entries with `"merged"` are dropped from the detail text entirely (per design.md's Decisions, this is a belt-and-suspenders case since `detect_unreconciled_tail_evidence` itself stops reporting a merged task). This finding-message branch, together with 1.2's enriched `reconcile_state`/`reconcile_pr_url` fields, implements requirement: Reconciliation outcome is recorded and reported.
- [x] 3.2 If every entry in `unreconciled` ends up `"merged"` (should not normally reach this function, but handle it defensively per 3.1's dedup case), do not emit an `unreconciled-tail-evidence` finding at all for that journal.
- [x] 3.3 Update the module docstring's `unreconciled-tail-evidence` description (~line 14-20) to mention that reconciliation is now attempted automatically and the finding's message reflects the outcome, so a future reader isn't misled by the pre-existing "reproduced 2026-08-12" framing implying it's still purely manual.

## 4. Tests

- [x] 4.1 In `tests/orchestrator/test_integrate_complete.py`, add a test class for `reconcile_unreconciled_tail_evidence` covering: a clean single-finding reconciliation opens a PR and returns `reconcile_state: "opened"` with the PR URL; a second call over the same finding/journal returns `"already-open"` and does not create a second PR; a finding whose PR has since merged (mock `gh pr view` returning MERGED) returns `"merged"`; a finding whose tail branch conflicts with base on merge returns `"quarantined"` with a merge-conflict reason and the same `QUARANTINE_MERGE_CONFLICT` constant used elsewhere; two findings in the same call are reconciled independently (one quarantined, one opened, in either order) and both outcomes are reflected correctly.
- [x] 4.2 Add a test confirming a quarantined synthetic `tail-<task-id>` group's journal entry is picked up by `quarantine_selfcheck.check_repo()` with zero changes to that module (asserts the design's "no dashboard change needed" claim as a regression test, not just a design note).
- [x] 4.3 In `tests/router/test_journal_selfcheck.py`, update/add tests for the enriched `unreconciled-tail-evidence` finding message: `reconcile_state: "opened"`/`"already-open"` → informational wording with the PR URL present in `detail`; `reconcile_state: "quarantined"` or absent → today's manual-triage wording; all-`"merged"` → no finding emitted.
- [x] 4.4 Add or extend a `live.py` integration-level test (mocking `integrate_module.reconcile_unreconciled_tail_evidence` or its `gh`/git seams, matching the existing style of tests around the `_pipeline_scheduler`/`_full_real_inner` tail-dispatch flow) confirming both call sites invoke reconciliation before recording, and that an empty findings list is a no-op (no `integrate_one` call, no journal write beyond the existing `_record_unreconciled_tail_evidence` clear-when-empty behavior).

## 5. Verification

- [ ] 5.1 [cleanup] Run `PYTHONPATH=src pytest -q` and confirm the full suite is green, including the new/updated tests from section 4.
- [x] 5.2 [cleanup] Run `PYTHONPATH=src python3 -m worktrail.orchestrator.orchestrate check` (golden record/replay regression) and confirm it stays green.
- [x] 5.3 [cleanup] Re-read `proposal.md` and `specs/tail-task-auto-reconciliation/spec.md`'s scenarios against the implementation and confirm each scenario has a corresponding test from section 4 (not just narrative coverage).
