## 1. Reproduce and fix the squash-merge dependency carry

- [ ] 1.1 Make the lifecycle harness's fake `gh pr merge` perform a real squash merge (tests/orchestrator/lifecycle/fake_gh.py), add a lifecycle-harness scenario with a `kind: e2e` tail task whose dependencies' groups are squash-merged and cleaned up before tail dispatch (tests/orchestrator/lifecycle/test_lifecycle_harness.py), confirm it reproduces `WorktreeMissingDependencyFileError`, then implement the carry: `add_stacked_worktree` gains optional `remote`/`base` params and a post-stack step that, for DONE dependencies with deleted branches and missing declared files, fetches and merges the freshest base ref with `-X ours` (src/worktrail/orchestrator/live.py); thread `remote`/`base` through `_pipeline_scheduler._ensure_wt`, `live_run_real`'s sequential `ensure_wt`, `_full_real_inner`, and `_dispatch_pending_tail`; the new scenario and the full existing suite pass.
